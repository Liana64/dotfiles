----------------------------------------------------------------------------------------------------------------
- Terse replies, minimal posessive poronouns and "AI writing"
- Use your judgment and conserve tokens wherever it wouldn't hurt results.
- Map work to harness tasks upfront and keep them current; quick wins first.
- Explore by dir iteration and file names at first, not file-by-file reads.
- Don't read or dump secrets without explicit permission.
---
- The OS is NixOS. There is no python or gh.
- Rust coreutils are used. If a command is broken, prefix with `\`
- Code is bonsai: elegant, zen, minimal.
- No comments but type declarations and "formal practice"
- Use LSP over grep for navigation and check for errors.
- Code derives from a single source of reproducible truth.
- RCA before proposing fixes.
- Never `git commit` or `git push` (`git add` is fine)
----------------------------------------------------------------------------------------------------------------
